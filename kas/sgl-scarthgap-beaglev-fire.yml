header:
  version: 14
  includes:
    - kas/yocto/scarthgap.yml
    - kas/machine/beaglev-fire.yml
    - kas/common.yml
    - kas/sgl/sgl.yml

repos:
  polarfire:
    branch:
    tag: "v2025.03"

local_conf_header:
    image_features: |
        IMAGE_FEATURES:append = " ${OPTIONAL_FEATURES}"

env:
    OPTIONAL_FEATURES: "" # override on cmdline
